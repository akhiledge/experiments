#include<stdio.h>
void insert_pos(int data, int position);/*inserts an element at a position in 
                                          the linked list*/ 
void insert_ele(int, int);/*inserts an element into linked list after 
                            a given element*/ 
struct node *search(int element);/*search for a given element in the linked 
                                   list and return the address of the node*/ 
void display(); /*display the elements in the linked list starting from 
                  start of the linked list*/ 
void delete_element(int data); /*remove an element from the linked list */ 

