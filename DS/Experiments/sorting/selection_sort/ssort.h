#ifndef _SSORT_H /*header guard for ssort.h*/ 
#define _SSORT_H

void selection_sort(int *a, int low, int high);/*sorts array using selection sort*/ 
void swap(int *a, int *b);/*to swap the values */ 
#endif /* ssort.h included*/ 
